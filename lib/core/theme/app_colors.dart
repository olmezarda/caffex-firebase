import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF8E5A1F);
  static const Color accent = Color(0xFFEACBCB);
  static const Color background = Color(0xFFFDF6F0);
  static const Color dark = Color(0xFF5D4037);
  static const Color yellow = Color(0xFFD0AD54);
  static const Color light = Color(0xFFCCCCCC);
  static const Color white = Colors.white70;

  static Color error = Colors.redAccent.withAlpha(200);
  static Color success = Colors.greenAccent.withAlpha(200);
}
